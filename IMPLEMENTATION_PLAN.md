# IMPLEMENTATION_PLAN.md
## Local BitNet Analyzer — Detailed Implementation Plan

> 목적: SPEC.md(=doomed 기반 스펙) 구현을 위한 단계별 작업 순서/완료 기준/리스크 정리  
> 원칙: **포용적/친절**, 자동 로드/너그러운 멀티 액션, 버튼 최소(반자동 2개), bitnetd 공용 엔진

---

## 0. 준비(저장소/규칙 고정)

### 0.1 저장소 초기 구조
- `bitnetd/` : FastAPI + SSE 서버
- `analyzer/` : pywebview 앱
- `shared/` : 공통 타입/프로토콜(카드, 메타, 요청/응답 schema)
- `docs/` : 사용자 가이드(수동 설치/복구/FAQ)

### 0.2 경로/상수(공통 규칙)
- BitNet 홈: `%LOCALAPPDATA%\BitNet\`
  - `bin/`, `models/`, `config/`, `cache/`, `logs/`
- Analyzer 앱 데이터: `%LOCALAPPDATA%\AnalyzerApp\`
  - `history.jsonl`, `results/`, `logs/`
- bitnetd 주소: `http://127.0.0.1:11435` 고정

✅ 완료 기준
- 저장소에 SPEC.md + IMPLEMENTATION_PLAN.md 존재
- 폴더 구조가 고정되고, 상수/경로는 shared에만 둔다(중복 금지)

---

## 1. bitnetd 최소 서버(모델 없이 먼저)

> 목표: “로컬 서버/클라이언트 생명주기/보안 토큰”부터 안정화  
> 모델 실행은 뒤로 미룸(초반 디버깅 지옥 방지)

### 1.1 FastAPI 서버 부팅
- 127.0.0.1에만 바인딩
- 포트 11435 고정

### 1.2 토큰 시스템
- `%LOCALAPPDATA%\BitNet\config\token.txt`
- 없으면 생성(랜덤 문자열)
- `/generate`, `/clients/*`는 토큰 필수
- `/health`는 토큰 없이 허용

### 1.3 /health (상태 머신만)
- status: ready / starting / not_ready / shutting_down / error
- reasons: engine_missing / weights_missing / token_required / model_load_failed ...
- client_count 포함

> 사용자 UI에는 상세 노출하지 않지만, 서버는 일관된 상태를 제공해야 함.

### 1.4 /clients 규격
- `POST /clients/register` (client_id + app_name)
- `POST /clients/heartbeat`
- `POST /clients/unregister`
- heartbeat 5초 / TTL 15초
- 응답은 단순 OK/ERR + client_count

### 1.5 종료 트리거
- client_count==0이면 즉시 종료(유예 0초)
- 진행 중 generate가 있으면 마무리 후 종료(현재 단계에서는 generate 미구현이라 즉시 종료 테스트만)

✅ 완료 기준
- `/health` 응답 정상
- 토큰 없으면 `/clients/register` 401
- register/heartbeat/unregister로 client_count 변화
- TTL 만료로 client가 자동 제거됨(좀비 방지)
- 0이 되면 서버가 종료됨(프로세스 확인)

⚠️ 리스크/체크
- 방화벽 규칙 추가하지 않기
- 0.0.0.0 바인딩 금지

---

## 2. Analyzer UI “껍데기” + 연결 성공 UX

> 목표: 앱 실행 → bitnetd 연결 → 사용자에게는 “연결 성공”만 보여주기

### 2.1 pywebview 앱 창 띄우기
- 상단: 새 채팅(+) / 세션 목록 토글 / 세션 삭제
- 본문: 대화 로그 영역
- 하단: 입력창(Enter 실행) + Shift+Enter 줄바꿈
- 📎 첨부 버튼(다중)

### 2.2 bitnetd 자동 실행
- Analyzer 실행 시 bitnetd 프로세스 체크
- 없으면 실행 시도(반자동)
- 연결 확인은 `/health`로 수행

### 2.3 연결 상태 표시(단순)
- 성공: ✅ 연결 성공
- 실패: ❌ 연결 실패 + 버튼 2개만 표시
  - Install/Build Engine
  - Download Model
- 상세 원인, /health 정보는 숨김(로그에만)

✅ 완료 기준
- Analyzer 실행 시 bitnetd가 떠있으면 “연결 성공”
- bitnetd가 없거나 실패면 “연결 실패” + 버튼 2개
- UI가 단순하고 버튼/설정 남발 없음

---

## 3. 세션 시스템(history.jsonl)

> 목표: Perplexity처럼 세션이 남고 전환/삭제가 된다

### 3.1 history.jsonl 이벤트(append-only)
- session_created
- message_added
- files_attached
- session_deleted

저장 위치:
- `%LOCALAPPDATA%\AnalyzerApp\history.jsonl`

### 3.2 세션 UI
- 새 채팅: 새 세션 생성
- 세션 목록: 토글로 보기
- 삭제: 세션 단위 삭제(1차는 메시지 단위 삭제 생략 가능)

### 3.3 세션 제목 규칙
- 기본: “새 채팅 n”
- 첫 질문이 들어오면 제목을 첫 문장 일부로 자동 갱신(선택)

✅ 완료 기준
- 앱 재실행해도 세션/로그가 유지됨
- 세션 전환 시 로그 복원
- 삭제 시 history에 기록(소프트 삭제 권장)

---

## 4. 파일 첨부 + 자동 로드(포용적)

> 목표: 사용자가 파일 첨부만 하면 “최대한 읽히게” 자동 처리

### 4.1 Dataset Registry
- 첨부 파일마다 dataset_id 부여
- active dataset = 가장 최근 첨부
- 다중 파일 targets 확장은 자연어로 수행

### 4.2 CSV 자동 인코딩
- utf-8-sig → utf-8 → cp949/euc-kr
- 성공하면 내부에 인코딩 기록(사용자 숨김)

### 4.3 공공데이터 헤더/잡행 자동 탐지
- 상단 N줄 스캔해서 “헤더 후보” 선택
- skiprows 자동 추정
- 컬럼명 정리(공백 제거, 중복 suffix)

### 4.4 XLSX 자동 처리
- 데이터가 가장 많은 시트 자동 선택
- 수식은 캐시된 값 우선
- 캐시 없으면 친절 안내(엑셀 열고 저장 후 재시도)

### 4.5 실패 정책(친절)
- 실패 시 자동 재시도 루트:
  - 다른 인코딩/다른 시트/헤더 재추정
- 최종 실패만 UI에 “로드 실패”로 표시
- 수동 가이드/Reset 루트 제공

✅ 완료 기준
- CSV/XLSX 대부분 자동 로드 성공
- 실패해도 “복구 루트(수동 가이드/리셋)”로 이어짐

---

## 5. 자연어 Router + 타겟 좁히기(너그럽게)

> 목표: 버튼 없이 자연어로 파일/시트/컬럼/범위를 지정하고, 애매하면 채팅으로 좁힘

### 5.1 Router 출력 고정(멀티 액션 지원)
- actions[] 형태
- action: intent, targets, args

### 5.2 타겟 규칙
- 파일: active / all / index / name fuzzy
- 시트: index / name fuzzy (xlsx)
- 범위: 상위 N행 / 샘플 N
- 비교/병합: compare/merge(merge는 key 없으면 질문)

### 5.3 컬럼 매칭기(강화)
- 정규화 강화(단위/괄호/케이스/구분자/숫자 접미)
- 동의어/약어 사전 확장(시간/좌표/센서/전기/ID)
- 매칭 흐름:
  - 자동 확정(격차 크면)
  - 애매하면 후보 3개
  - “아니다”면 2차:
    1) 키워드 더 정확히
    2) 관련 10개 제시
    3) 유사 전부로 할지 확인
  - 그래도 실패면 새 질문 유도 + 예시 3개

### 5.4 타겟 좁히기 대화 관리
- 질문/응답도 history에 저장
- 좁히기 과정에서 가능한 작업은 먼저 수행(부분 진행)

✅ 완료 기준
- 컬럼 100~200개 환경에서도 무리 없이 타겟 지정
- 애매할 때 종료하지 않고 대화로 좁혀감(포용적)

---

## 6. Core 분석 intent 구현(모델 없이 가능한 것 먼저)

> 목표: LLM 없어도 “분석 기능”이 돌아가야 함 (LLM은 설명/해설 강화)

### 6.1 1차 지원 intent (총 11개)
- summary, schema, filter, aggregate, compare, plot, export, help, columns, preview, validate

### 6.2 구현 순서(추천)
1) columns / preview → Table 카드
2) summary / schema / validate / help → Text(+작은 표)
3) filter → Table (또는 요약 + 샘플)
4) aggregate → Table
5) compare → Text + Table(차이 요약)
6) plot → Chart 카드(Phase 7에서 강화)

✅ 완료 기준
- 각 intent가 최소 기능으로라도 동작
- 결과는 카드(Text/Table/Chart)로만 반환

---

## 7. 시각화 강화(분석 느낌)

> 목표: “그래프 그려줘”만 해도 2~4개 차트 패키지 자동 생성

### 7.1 기본 차트 패키지(가능한 것만 생성)
- 숫자: 히스토그램
- 범주: Top20 막대
- 결측률: Top20 막대
- 시간 컬럼 있으면 시계열(자동 집계 단위 선택)

### 7.2 차트 카드 규칙
- PNG로 저장
- UI 썸네일 + 클릭 확대
- 카드 아래 메타(작게): 사용 컬럼/집계/기간 등

✅ 완료 기준
- plot 결과가 눈으로 “분석 느낌”이 나게 나옴
- 기본 Top N = 20

---

## 8. bitnetd /generate (Transformers + SSE)

> 목표: “로컬 LLM 설명/해설/포용적 질의” 완성

### 8.1 Transformers 모델 로드
- GPU 우선, 실패 시 CPU fallback
- 모델 기본값: microsoft/bitnet-b1.58-2B-4T (HF snapshot)
- 모델은 huggingface_hub snapshot_download로 설치/재개(resume)

### 8.2 /generate 구현(확정 스펙)
- options 8개 지원
- analysis_default 보수적 프리셋(서버 기본)
- stream=true:
  - SSE: meta/delta/done/error
  - delta는 텍스트만
  - done은 최종 텍스트 + 메타 포함
- stream=false: JSON {text, meta}

### 8.3 Analyzer SSE 수신
- 스트리밍으로 답변 렌더
- 메타는 결과 아래 작게 표시

✅ 완료 기준
- LLM 응답이 스트리밍으로 자연스럽게 출력
- 메타가 남고 재현성/디버깅 가능

---

## 9. Export(저장) — 요청 있을 때만 + 파일 카드

> 목표: 자연어로 저장 요청하면, 파일 카드로 결과 반환

### 9.1 카드 번호(#1,#2,...) 체계
- 한 쿼리 결과 카드에 번호 부여
- “2번 표 저장” 같은 참조 가능

### 9.2 저장 범위 해석
- 기본: 이번 쿼리(직전 턴)
- “방금 시각화만”
- “세션 전체”
- “n번 카드만”

### 9.3 포맷 지원
- xlsx/csv/png/md/json
- xlsx 묶음 시트 규칙:
  - INDEX / SUMMARY / TABLE_01.. / CHARTS / META
- 세션 전체 저장 기본: md+json, xlsx는 요청 시

✅ 완료 기준
- 저장은 요청 있을 때만 수행
- 결과는 파일 카드로 반환

---

## 10. 패키징/배포(완성 느낌)

> 목표: Analyzer.exe + bitnetd.exe 로컬 빌드 + 포터블/설치형 지원

### 10.1 PyInstaller
- Analyzer → exe 패키징 (포터블 기본 + 설치형 옵션)
- bitnetd → 로컬 빌드로 exe 생성 후 `%LOCALAPPDATA%\BitNet\bin\`에 배치

### 10.2 반자동 설치 UX
- Install/Build Engine 버튼이 bitnetd.exe 준비
- Download Model 버튼이 HF snapshot 설치

### 10.3 수동 가이드 강화(친절)
- 어디서 받는지(원칙)
- 어디에 넣는지(bin/models/config)
- 무엇을 실행하는지
- Reset Engine/Model/All

✅ 완료 기준
- 사용자(초보)도 “프로그램 켜는 순간까지” 막히지 않게 안내 가능
- 꼬이면 리셋/복구 루트 제공

---

## Recommended Order (최단 성공 루트)
1) Phase 1(bitnetd 뼈대)  
2) Phase 2(Analyzer 연결 성공 UI)  
3) Phase 3(세션)  
4) Phase 4(자동 로드)  
5) Phase 5(자연어 타겟/좁히기)  
6) Phase 6(Core intent)  
7) Phase 7(시각화 강화)  
8) Phase 8(bitnetd LLM + SSE)  
9) Phase 9(Export)  
10) Phase 10(패키징)

---
